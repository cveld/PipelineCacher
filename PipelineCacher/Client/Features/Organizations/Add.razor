@using Entities
<h3>Add</h3>

<button @onclick="AddClicked">Add</button>
@code {
    [CascadingParameter] BlazoredModalInstance BlazoredModal { get; set; }

    async void AddClicked()
    {
        await BlazoredModal.CloseAsync(ModalResult.Ok(new Organization
        {
            Name = "Test"
        }));
    }
}
